````
README

- 中文

系统账号：yahboom

系统密码：yahboom

```
如果需要将代码移植到自己的电脑上运行时,需要将此文件夹下的libdofbot_kinemarics.so动态库放在/usr/lib/目录下.
dofbot_ws和catkin_ws工作空间下的动态库libdofbot_kinemarics.so是在jetson nano系统中使用的.
特别注意:此文件夹下的动态库libdofbot_kinemarics.so与dofbot_ws和catkin_ws工作空间内的动态库libdofbot_kinemarics.so,名字相同,使用环境不同.
```

- English

System account: Jetson

System password: yahboom

```
If you need to migrate the code to your own computer, you need to transfer the libdofbot under this folder when running_ kinemarics. The so dynamic library is placed in the [/usr/lib/] directory

dofbot_ WS and catkin_ Dynamic library libdofbot in WS workspace_ kinemarics. So is used in the Jetson nano system

Special note: the dynamic library libdofbot under this folder_ kinemarics. So and dofbot_ WS and catkin_ Dynamic library libdofbot in WS workspace_ kinemarics. So, the name is the same, but the use environment is different
```
````