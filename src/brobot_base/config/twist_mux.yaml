twist_mux_node:
  ros__parameters:
    topics:
      navigation:
        topic   : cmd_vel
        timeout : 0.5
        priority: 200
      tracker:
        topic   : cmd_vel_tracker
        timeout : 0.5
        priority: 240
      joystick:
        topic   : cmd_vel_joy
        timeout : 0.5
        priority: 255
